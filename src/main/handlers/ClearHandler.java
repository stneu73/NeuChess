package handlers;

import spark.Request;
import spark.Response;

public class ClearHandler {
    public Object ClearHandler(Request req, Response res) {
        //request json to request object (not for clear)
            //have tell gson what to deserialize to if handler has a request
        //pass object to service
        //set status code based on response object
        //res.status(value)
        return //response object to response json
    }
}
